import { IngredientTypePipe } from './ingredient-type.pipe';

describe('IngredientTypePipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientTypePipe();
    expect(pipe).toBeTruthy();
  });
});
