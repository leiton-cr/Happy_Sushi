import { MissingIngredientDirective } from './missing-ingredient.directive';

describe('MissingIngredientDirective', () => {
  it('should create an instance', () => {
    const directive = new MissingIngredientDirective();
    expect(directive).toBeTruthy();
  });
});
